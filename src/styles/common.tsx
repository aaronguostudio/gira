import styled from '@emotion/styled'
import { Button } from 'antd'

export const STYLES = {
  pageHeader: '6rem',
  shadow1: 'rgba(0, 0, 0, 0.1) 0 0 10px'
}

export const LongButton = styled(Button)`
  width: 100%
`

